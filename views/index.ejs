<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Pokedex App</title>
  </head>
  <body>
    <nav>
      <a href="/pokemons/new">Create a New Pokemon</a>
    </nav>
    <h1>Pokedex index page</h1>
    <ul>
      <% for(let i = 0; i < allPokemons.length; i++) { %>
      <li>
        <a href="/pokemons/<%=i%>"><%=allPokemons[i].name %></a>
        <img src="<%= allPokemons[i].img %>" alt="pokemon image" />
        <form action="/pokemons/<%=i; %>?_method=DELETE" method="POST">
          <input type="submit" value="DELETE" />
        </form>
      </li>
      <% } %>
    </ul>
  </body>
</html>
